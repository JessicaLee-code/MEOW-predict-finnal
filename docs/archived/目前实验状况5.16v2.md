# 目前实验状况 5.16 V2

日期：2026-05-16  
项目：MEOW 金融时序预测 V3.3  
用途：给老师汇报当前正式 rolling 结果和下一步实验方向

## 1. 当前结论先说

当前实验已经比较明确地说明一件事：

- 复杂模型没有稳定超过最朴素的 `B0_ridge_legacy`
- 当前最强可复现结果仍然停留在 `0.048` 左右
- 距离 `0.10` 还有明显差距

所以现阶段不能再把重点放在继续堆复杂模型上，而应该放在：

1. 找到正式 rolling 下最稳定的 backbone
2. 解释 legacy 特征为什么比 core 更强
3. 只做受限特征增强和受限融合

## 2. 正式 rolling 设置

本轮正式复验统一使用：

- 5 折 formal rolling
- `train_window=8`
- `val_window=2`
- `step=10`
- `embargo=1`
- `max_train_days=4`
- 统一切分、统一随机种子、统一特征处理

评价指标采用：

```text
stability_score = rolling_corr_mean - 0.7 * rolling_corr_std
```

同时记录：

- `rolling_corr_mean`
- `rolling_corr_std`
- `rolling_corr_min`
- `rolling_mse_mean`
- `rolling_r2_mean`
- `daily_corr_mean`
- `daily_corr_std`

## 3. Backbone 复验结果

### 3.1 正式 rolling 汇总

| experiment | rolling_corr_mean | rolling_corr_std | rolling_corr_min | stability_score |
| --- | ---: | ---: | ---: | ---: |
| `B0_ridge_legacy` | 0.036365 | 0.011488 | 0.025569 | 0.028323 |
| `B6_common_residual` | 0.044205 | 0.022870 | 0.016618 | 0.028196 |
| `B7_soft_regime` | 0.041538 | 0.021872 | 0.004875 | 0.026228 |
| `B8_ridge_legacy_plus_core` | 0.037219 | 0.031576 | -0.014326 | 0.015116 |

### 3.2 判断

- `B0_ridge_legacy` 仍然是最稳的 backbone
- `B6_common_residual` 和 `B7_soft_regime` 只是在个别 fold 有高点，但整体不稳定
- `B8_ridge_legacy_plus_core` 没有稳定超过 `B0`，而且波动更大

## 4. Ridge 特征增强结果

### 4.1 正式 rolling 汇总

| experiment | rolling_corr_mean | rolling_corr_std | rolling_corr_min | stability_score |
| --- | ---: | ---: | ---: | ---: |
| `R00_ridge_legacy` | 0.036365 | 0.011488 | 0.025569 | 0.028323 |
| `R01_ridge_legacy_plus_core` | 0.037219 | 0.031576 | -0.014326 | 0.015116 |
| `R02_ridge_legacy_plus_norm_core` | 0.047976 | 0.014975 | 0.032599 | 0.037494 |
| `R03_ridge_legacy_plus_patch_summary` | 0.026921 | 0.037583 | -0.036456 | 0.000613 |
| `R04_ridge_legacy_plus_cross_rank` | 0.036891 | 0.015109 | 0.014107 | 0.026314 |

### 4.2 判断

- 当前最好的 Ridge 增强方向是 `legacy + normalized core`
- 原始 `core` 直接并入后，稳定性变差
- `patch_summary` 和 `cross_rank_features` 没有形成稳定增益

## 5. 为什么 legacy 比 core 强

### 5.1 legacy 中而 core 中没有的特征

- `ob_imb0`
- `ob_imb4`
- `ob_imb9`
- `trade_imbema5`
- `lagret12`

其中 `trade_imb` 同时出现在 legacy 和 core 中。

### 5.2 特征含义

- `ob_imb0` / `ob_imb4` / `ob_imb9`：不同深度的订单簿 imbalance
- `trade_imbema5`：成交 imbalance 的平滑版本
- `lagret12`：12 分钟收益偏离，反映中短周期路径信息

### 5.3 Ridge 系数摘要

| feature | mean_abs_coef | mean_abs_rank | sign_consistency | fold_stability |
| --- | ---: | ---: | ---: | ---: |
| `trade_turnover_imb` | 0.0006136 | 7.20 | 0.8 | 0.99940 |
| `trade_imb` | 0.0005949 | 2.60 | 0.8 | 0.99973 |
| `ob_imb4` | 0.0005549 | 4.80 | 0.6 | 0.99946 |
| `obi4` | 0.0005539 | 4.60 | 0.6 | 0.99946 |
| `obi0` | 0.0004580 | 7.20 | 0.8 | 0.99940 |
| `ob_imb0` | 0.0004469 | 8.20 | 0.8 | 0.99942 |
| `order_pressure` | 0.0003407 | 7.20 | 0.8 | 0.99971 |
| `trade_imbema5` | 0.0002740 | 6.00 | 0.4 | 0.99986 |
| `lagret12` | 0.0001404 | 11.80 | 0.6 | 0.99984 |

### 5.4 初步解释

legacy 更强，主要不是因为更复杂，而是因为它更贴近：

- 订单簿 imbalance
- 成交 imbalance
- imbalance 的平滑形式

这说明当前有效信号更偏向“供需压力”和“成交方向性”，而不是更复杂的非线性结构。

## 6. OFI 新信号族 audit

本轮按计划测试了 OFI 动态订单流信号族，基线仍然是 `R02_ridge_legacy_plus_norm_core`。

### 6.1 OFI 结果

| experiment | rolling_corr_mean | rolling_corr_std | rolling_corr_min | stability_score |
| --- | ---: | ---: | ---: | ---: |
| `O1_R02_plus_ofi_raw` | 0.046675 | 0.015482 | 0.032123 | 0.035838 |
| `O2_R02_plus_ofi_dynamic` | 0.046446 | 0.015820 | 0.029417 | 0.035372 |
| `O3_R02_plus_ofi_rank` | 0.047155 | 0.015148 | 0.032880 | 0.036552 |
| `O4_R02_plus_ofi_safe` | 0.045898 | 0.014972 | 0.026719 | 0.035417 |
| `O5_R02_plus_ofi_raw_dynamic` | 0.046548 | 0.015717 | 0.029496 | 0.035546 |
| `O6_R02_plus_all_ofi` | 0.045898 | 0.014972 | 0.026719 | 0.035417 |

### 6.2 判断

- OFI 有信号，但没有超过当前最好的 `R02_ridge_legacy_plus_norm_core`
- 最好的 OFI 版本是 `O3_R02_plus_ofi_rank`
- 所以 `trade_impact` 和 `conditional_momentum` 暂时不应继续扩展成主线

## 7. 训练窗敏感性结果

为了验证 `max_train_days=4` 是否压制性能，本轮做了一个最小正式对照：

- `max_train_days=4`
- `max_train_days=80`

### 7.1 结果

| experiment | rolling_corr_mean | rolling_corr_std | rolling_corr_min | stability_score |
| --- | ---: | ---: | ---: | ---: |
| `R02_ridge_legacy_plus_norm_core_max_train_4` | 0.039211 | 0.002293 | 0.036918 | 0.037606 |
| `R02_ridge_legacy_plus_norm_core_max_train_80` | 0.026005 | 0.016894 | 0.009110 | 0.014179 |

### 7.2 判断

- 这次对照里，`max_train_days=4` 反而比 `80` 更稳
- 说明“训练窗越大越好”并不成立
- 更长训练窗可能引入过多旧分布样本，稀释当前有效信号

## 8. 当前阶段结论

当前已经可以比较明确地说：

1. `B0_ridge_legacy` 仍然是最稳 backbone
2. `R02_ridge_legacy_plus_norm_core` 是当前最强 Ridge 增强方向
3. OFI 有增量，但还不足以替代 backbone
4. `max_train_days=4` 这次没有压制性能，反而比 `80` 更好

## 9. 下一步建议

接下来建议只做三件事：

1. 继续围绕 `B0` 和 `R02` 做受限增强，不再盲目扩复杂模型
2. 深挖 `legacy` 和 `normalized core` 为什么有效，判断哪些是稳定信号，哪些只是噪声
3. 如果要继续冲更高分，优先检查特征对齐、切分、泄漏和信号编码，而不是直接加更复杂的模型结构

## 10. 可直接向老师汇报的一句话

> 当前正式 rolling 下最稳的 backbone 仍然是 `B0_ridge_legacy`，最好的 Ridge 增强是 `legacy + normalized core`，OFI 有一点增量但没有超过当前最佳，训练窗方面 `max_train_days=4` 反而比 `80` 更稳。现在更关键的是继续找稳定信号源，而不是继续堆复杂模型。

