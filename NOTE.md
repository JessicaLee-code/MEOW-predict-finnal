# NOTE.md — 实验笔记（DL 主线）

> 给我自己看的研究日志：只记最近敲定的 DL 设计**为什么这么定**。规则正文在 `AGENTS.md`，任务看板在 `CLAUDE.md`，完整架构在 `docs/specs/DL实验设计规格.md`。
> 传统/线性那一整套（评测口径、特征侧 P1–Q4、转树、选模型协议）的笔记留在传统分支，本分支不再展开。

---

## 战略：直接冲 0.12，不验"序列有没有料"

老师明示有人做到过 0.12、业界 0.2–0.3 → 序列有信号是**先验事实**，再花预算去"验证存在性"是浪费。所以 DL 起手就奔 0.12，只保留"别骗自己"的评测纪律。隐藏集分布未知（甚至不确定是不是时间外推）→ **鲁棒性优先**，冲的是"可辩护、可泛化的分"，不是 dev 刷高。传统侧已基本可提交、是保底代表，DL 是上行主线（不是"打不过就回退"的赌注）。

## 架构：固定脊柱 + 可换卡带

把**评测协议/窗口切分/归一化/指标/配置**做成不可变脊柱，把**输入适配(InputAdapter)**和**模型本体(ModelCartridge)**做成可换卡带。为什么这么切：换模型（LSTM-on-features → DeepLOB-on-rawLOB）时只动两块卡带，脊柱一行不改——所有"理解原始数据语义"的活关进 InputAdapter，它一旦吐出通道布局固定的 `[n_interval, C]`，下游全是不关心含义的张量数学。

**PyTorch 封死在卡带内**（torch import / nn.Module / optimizer / GPU / 训练循环都在 `fit/predict` 里），脊柱全程 torch-free、不碰一个 tensor。这是脊柱能复用、能换模型的根。

## 评测：海选 + expanding 两段（不是三层 holdout）　〔⚠️ 2026-06-01 已被新协议取代，见下「评测协议为什么这么重设计」；本节保留为历史〕

DL 有两类盲区、配两类工具：① **单窗口内**靠 train vs 早停验证两条曲线就能看出过拟合（自带探测器）；② **walk-forward 多折**探测时间不稳定性（撞运气）。所以最优分配 = 单窗口看曲线 + 少量 expanding 折看稳定性，不堆很多折（DL 贵、数据有限）。

落成两段预算：**海选**（单切分 + 早杀，便宜分诊大量 config）→ **expanding 认证**（少折 walk-forward + 多种子，最终确认）。**不再用传统"三层 Dev/Review/Final"**——第三层 12 月从来没执行过，新协议已用 海选+expanding 取代分层。防自欺只留内核：config-lock（获胜超参在交付演练前冻结）、少看 expanding（看多会过拟合）、最终确认数据看完即定不回灌。

## 训练生命周期：内层归 PyTorch，外层归框架

内层 epoch 循环/optimizer/早停归卡带（PyTorch）；外层协议（折切分、算指标）归脊柱。**早停看卡带自己的"早停验证集"**（训练区尾段切出、与打分集隔离），可用代理指标——这样不会对最终上报的打分集过拟合。训练**之中无数据回流脊柱做控制**；卡带只通过 TrainRecord（逐 epoch 曲线）**单向上报**，供脊柱判过拟合 + HPO 早杀整个 trial。官方 4 指标在 `predict()` 之后由脊柱算一次。

## 超参搜索：只搜结构 3 个旋钮　〔⚠️ 2026-06-01 升级，见下「选型粒度」：结构为主 + 小 HPO + seed 探针 + 其余固定重正则；本节保留为历史〕

只搜 **序列长 / hidden / 层数** 三个结构超参，其余冻默认；随机搜索 ≫ 网格；早杀坏 trial；训练行采样降成本；海选少种子、认证多种子。理由：结构才是主战场，省下的算力砸给"试更多结构"，而不是在一个结构上精调一堆次要旋钮。

## 配置管理：分布式声明 + 中央组装

每层声明自己职责的配置块，Orchestrator 只**组装**成一份 frozen `RunConfig` + 校验 + 冻结 + 派发。不写成 god-object（否则换模型还得回头改 Orchestrator，泛化性就废了）。三件事拆开别糊：**①合法词表(枚举)集中**在各配置块文件顶部、**②实现注册(枚举→类)**挨着实现放 registry、**③本次选了哪个值**进 RunConfig。cartridge 用 `required_adapter: AdapterKind` 类型化引用，不写裸字符串（拼错当场报错）。

**run_id 手工语义命名**（`日期_阶段_模型_意图_版本`，看得懂），哈希降级成 `config_fingerprint` 字段防"复用同名却悄悄改了配置"。纯哈希看不懂、纯手工易漂移，这样两头都占。

## 数据实情 → 路线收敛：DeepLOB 退役，改 TCN-on-原始微结构

接 PyTorch 前先查了真实 h5（`data/20230601.h5`，62 列）才定路线，不是拍脑袋：

- **没有连续 LOB**：盘口只给 4 个稀疏聚合档位（`bid0/4/9/19` + `ask` 同构），外加聚合 size（`bsize0 / bsize0_4 / bsize5_9 / bsize10_19`）与 turnover ratio（`btr0_4 / atr0_4 …`）。**不是 DeepLOB 假设的连续 10 档价量网格**——照搬 DeepLOB 的「10×2 网格 + Inception」根本没有对应输入，**DeepLOB-on-rawLOB 退役**。
- **真正能喂的 = ~59 个原始微结构通道**：价（`midpx / lastpx / OHLC` + 4 档买卖价）、量（聚合 size / turnover ratio）、订单流（主动买卖的笔数/量/额/高低/vwad，挂单、撤单同构）。
- **序列粒度**：每票每日 ~226 个 interval（日内 bar），即「序列」= 某票某日 226 步日内路径，**绝不跨日**（脊柱 WindowIndexer 已物理保证）。

→ 收敛成一句话：**第一个猛药 = TCN，喂清洗过的原始微结构通道（RawChannelAdapter），让网络自己学通道间交互**（对照 FeatureAdapter 喂 433 手工特征那条线）。

## 为什么 TCN（不是 Transformer，也不是堆 LSTM）

先把决策拆成两件正交的事：**喂什么（输入）** 和 **用什么结构（架构）**。我的判断——**输入/归一化/正则是大杠杆，架构是小杠杆**：同一份干净输入下 TCN / LSTM / GRU 的天花板差距，远小于「输入对不对、归一化漏没漏未来、正则够不够」带来的差距。所以架构就选「训练便宜 + 因果天然 + 局部模式归纳偏置对路」的那个，把省下的算力砸去搜结构、试输入。

TCN 胜出三条：
1. **因果是结构自带的**（causal dilated conv，padding 只补左侧）——不靠我手动 mask 防未来泄漏，和脊柱「窗末因果对齐」叠成双保险。
2. **训练并行、省 GPU**：卷积沿时间并行，不像 RNN 必须串行 BPTT；4060 预算紧，这条很值（省下的跑更多结构搜索）。
3. **归纳偏置对路**：膨胀卷积的多尺度局部感受野，正贴「订单流的短程脉冲 + 多尺度累积」；226 步路径用膨胀卷积几层就能覆盖全程。

Transformer 不选：注意力 O(L²)、小数据易过拟合，还得额外加因果 mask + 位置编码，这个数据规模性价比不如 TCN。LSTM 留作 **D1 低风险对照**（喂 433 特征当序列），不是主攻。经验背书：Bai/Kolter 2018《An Empirical Evaluation of Generic Convolutional and Recurrent Networks for Sequence Modeling》——多数序列任务 TCN ≥ LSTM/GRU。

> 落地分工：`TCN` 进 `ModelKind` 词表，卡带本体（nn.Module / 训练循环）是 torch，等 4060；现在能做的（RawChannelAdapter + Orchestrator + HPO Searcher + 全套 torch-free 测试）先做完，接卡那天只剩写 `TCNCartridge.fit/predict`。

## RawChannelAdapter 的语义归一（为什么只做最小变换）

「喂原始」≠ 裸喂，但也**不该在 adapter 里手搓 imbalance/OFI**——那是 FeatureAdapter（433 手工特征）干的活；走 raw 这条线的全部意义就是**让 TCN 自己学交互**。所以 adapter 只做「平稳化 + 量纲驯服」的最小语义层，统计白化仍交脊柱 Normalizer：

- **价 → 相对 mid**：各档买卖价 / OHLC / 成交高低 / vwad 取 `p/midpx - 1`（行内、无状态、平稳、无量纲）；成交价为 0（该 interval 无成交）时置 0（中性，避免 `(0-mid)/mid=-1` 的假信号）。
- **midpx 本身 → 日内对数收益**：按 `(symbol)` 组内 `log(midpx_t) - log(midpx_{t-1})`，首 interval=0。这是**最关键的收益路径通道**，因果（只用 ≤t）、不跨日不跨票。
- **量 / 笔数 / 额 → log1p**：size / turnover / `nTrade*` / 挂撤单量额都是非负厚尾，`log1p` 驯尾。

边界口径沿用规格 §2.3：**语义归一在 adapter，统计白化在脊柱 Normalizer**。RawChannelAdapter 全程 torch-free、行内/组内无状态变换，无跨日跨票泄漏风险，可在 Mac 上对真实 h5 直接验。

## 喂数据：DL 喂"原始" ≠ 裸喂

序列模型输入是 LOB/成交的时间路径，但必须先**平稳化**（价转收益、量转相对不平衡）+ **归一化**（前日/滚动 z-score）+ **防泄漏开窗**；归一化是最易翻车的一步。标准张量是 `[B, L, C]`，我们的"序列" = 某票某日的**日内 interval 序列**，**绝不跨日**。当前契约锁单张 `[L,C]`（覆盖 LSTM-on-features 与 DeepLOB），多粒度/跨票图结构留扩展缝（InputAdapter 输出放宽成张量字典，仅窗口器+归一化器需泛化）。**语义归一（如按 mid-price 相对化）放 InputAdapter，统计白化放脊柱 Normalizer**，边界这么分最干净。

数据要点（影响 DL 开窗/归一化，细节见传统分支）：`fret12` = 简单收益、不跨夜、厚尾（峰度~15）、每截面~300 票、主路径无结构泄漏。

## 防泄漏：numpy 参考模型当探测器

一个 torch-free 的 numpy 参考模型（恒 0 / 末 interval 线性）走**完整脊柱管线**必须打**低分**；打高分 = 脊柱漏了未来信息（窗口/归一化/标签对齐有 bug）。这是 D0 的核心验收闸。

---

## 路线再收敛（2026-06-01）：DL 押"截面"，TCN-on-raw 否决

多轮讨论后把路线钉死了，纠正前面几条浅层判断：

- **数据再核实**：无连续 LOB 是定论（规格 §8.0）。关键是——TCN 吃的那 **59 个"原始通道"和 GRU 要吃的 433 特征是同一份分钟聚合数据**，前者只是少做了特征工程。**raw 这条线没有更细、更富的数据**，所以"DL-on-raw 能挖出手工特征看不到的东西"这个赌注底子很薄；DeepLOB / raw-LOB 终局**不存在**。
- **TCN 表现差的真因 = 截面盲区，不是 max_epochs**（那是上一轮 Sonnet 的浅层归因，已修正）。当前张量契约 `[B,L,C]` 一次只看一只票，**跨票截面信息根本进不来**（规格 §303"缺邻接关系输入"）。RawChannelAdapter 又只做每票自身的相对价/对数收益，**没有任何截面归一**——于是 TCN 永远不知道"这只票今天在全市场排第几"，而传统那 0.0776 里很大一块 alpha 恰恰是截面的（cross-z / cross-rank）。TCN 不是"难学一点"，是**整整瞎掉了一个维度**。
- **数据形状把答案逼向截面**：行数 ≈ 309×226×125 ≈ 870 万，不饿；真正饿的是**时间轴**（146 天、锁死、补不了），而**截面轴每天 309 票、极其充裕**。所以 DL 唯一喂得饱、且正好是传统只用死板 z/rank 糊过去的轴，就是**截面**。
- **同学 MLP 4–5% 信息量≈0**：外部管线、裸两层、单个非 rolling val、且**不是序列模型**——对我们的瓶颈无参考价值。它最多说明"裸表格 MLP 不是突破口"（本就知道）。**我们自己的 features-DL 一次都还没跑过**，陪审团没进场。

## 还能 alpha 的全图（2026-06-01）

把所有可能出 alpha 的地方摊开，标清用/弃/为什么——给自己一张完整账本，防遗漏：

| 类别 | 来源 | 裁决 | 理由 |
|---|---|---|---|
| 表示 | 个股时序（日内路径） | ✅ 用（基线 token） | GRU 编码每票序列，序列腿吃进来 |
| 表示 | **截面联合（309 票/天）** | ✅ **主攻** | 唯一数据管够的轴；传统只用死板 cross-z 糊过 |
| 表示 | raw 微结构 | ❌ 死 | 无 LOB；与 433 同源、更少加工；TCN 已证瞎掉截面 |
| 表示 | 多分辨率 / 跨日上下文 | ⏸ 低优先 | 数据封顶 125 天；rolling 特征已吃掉一部分 |
| 表示 | 共因子 / 大盘均值（cross-z 丢掉的那块） | ◐ 便宜一臂 | 池化指标奖励这块；聚合流量预测截面均值方向 |
| 目标 | **直接优化评分 `MSE+λ·corr`** | ✅ 免费杠杆，随车 | 顶老师 Pearson 的 1/3，传统结构上做不到 |
| 目标 | 多 horizon 辅助监督 | ◐ 可选 | 弱正则，有空才加 |
| 目标 | 样本效率（预训练 / 重正则） | ✅ 截面配套 | 小时间数据下解锁 DL 的关键 |
| 目标 | 标签工程（winsorize / 可还原变换） | ☑ 已锁 | 传统侧定死，沿用 |
| 组合 | **集成去相关（DL + 传统）** | ✅ 保底 + 保险 | 最高概率稳定提升；兜住 DL 时段崩盘 |
| 特征 | 新微结构族（多窗 OFI / 成交方向自相关 / 已实现波动 …） | ◐ 双吃但递减 | 喂得动两边；传统已收口、边际小 |

**真正有肉的五个**：截面（主攻）、时序（基线）、`MSE+λ·corr`（免费）、集成（保底+保险）、共因子（便宜一臂）。其余要么死、要么封顶、要么递减。

**冲 0.10 的诚实账**：原来 DL 冲分有两条腿——(a) 对同一批特征用更强函数类、(b) 直接吃 raw 微结构。**(b) 因无 LOB、数据聚合而不存在**；(a) 又被裸 MLP 4–5% 削弱。所以 0.10 现在只能来自**把固定数据用得更聪明**：截面联合建模（吃充裕的那根轴）+ 直接优化评分 + 集成去相关 + 样本效率。**靠"scale 更多数据"那条路，因数据锁死 146 天，已经关了。**

## 交付 = 方法非权重 → 评测协议为什么这么重设计（2026-06-01）

> 规则正文已落 `AGENTS.md §十一`，这里只记"为什么"。

- **老师现场重训重评 → 考的是方法跨时间泛化**，不是权重。于是 **rolling 的每一折 = 老师那场考试的一次完整复现**（train 过去 → predict 紧邻未来）；折间一致性就是被打分的那个量。所以"必须做 rolling"不是规范洁癖，是**rolling 本身就是考试**。
- **数据稀缺的平衡点**：你不能从 146 个非平稳交易日里造出很多独立折——别和重合较劲。最优 = **锚定扩展**（每折训练最长、忠于部署）+ **测试段不重合**（regime 覆盖）+ purge/embargo（堵泄漏）+ **最坏折 + block-bootstrap** 老实读数。**训练重合不是 bug**（它就是部署现实），只破坏"把折当独立样本"的幼稚 CI；对选型无害（公因子抵消）。CPCV 是榨数据好工具但**非严格因果，只配副 sanity**。
- **DL vs 传统的时段过拟合**：DL 风险**明显更高**（容量大 / 自学表示无先验锚 / 优化随机，TCN 种子方差 0.068、R² 全负为证）。但容量是双刃——也是它唯一能赢传统的本钱。所以三层防御：**rolling 检测 / 重正则 + 多种子降低 / 与传统集成兜底**。传统不是免疫（树在 expanding 上"塌了"就是判例），只是暴露小；这个判例对 DL 只会更适用。
- **用 Dec**：交付是方法、不冻权重 → "从未碰过的 Dec lockbox"这个概念已不适用。Dec 就当 rolling 最近的一折用满；防过拟合改靠"小候选集 + 最坏折 + 网格跑一次不反复重调"。

## 截面模型怎么设计：两条腿融合的拍板（2026-06-01）

> 主攻方向已定（押截面），这里把"模型长什么样、时序信息和截面信息怎么融进一条管线"拍死。契约/落地规则进规格 §8.2。

**一句话结构**：**共享时序腿（GRU 编码每票日内路径 → 一个向量）→ 截面腿（同一 `(date,interval)` 上对当时在场的 ~309 票做 set-attention、跨票互看）→ per-票头出预测**；截面腿**残差**接（学不到就退化回纯时序基线）。

**为什么"先时序后截面"的因子化两腿，而不是时空联合大注意力**：
- 业界共识就这么切——交通/时空预测（STGCN、Graph WaveNet、DCRNN）、量化选股（Feng 2019 关系排序 RSR、HIST）全是**时序编码 per 节点 + 截面/图混合**的因子化结构，没人做 O((N·L)²) 的时空联合注意力（参数爆炸、在 146 天小数据上必过拟合）。N=309、L=32 联合 ≈10⁷/截面，免谈。
- 因子化让两腿各司其职、参数省：时序腿**一套权重所有票共享**（≈870 万行全喂一个编码器 → 样本效率拉满，这正是 DL 在我们数据上唯一喂得饱的吃法）；截面腿只在低维编码向量上跨票，便宜。

**为什么截面腿用 set-attention（全连接自注意力），不用 GAT/GCN 图网络**：
- 我们**没有可靠邻接图**（无行业/供应链干净边）。全连接自注意力 = 让模型**自己从数据学软图**（等价 Graph WaveNet 的自适应邻接）。
- set-transformer **无位置/无身份编码** → 天然**置换等变 + 零股票身份**（§十一·11.5 重正则要的那两条免费拿到）：一只票只由它的时序特征表示、永不靠 ID embedding → **换池子也不崩**（老师万一换股票池的免费保险）。

**为什么残差接**（`z_i = h_i + CrossAttn({h_j})_i`）：截面腿学不到东西时残差≈0 → 优雅退化回纯时序 GRU 基线（=③那条腿）。这天然把 ③ 做成 ② 的**消融下界**：③（纯时序）先跑出底，②（加截面腿）的增量就干净地回答"截面联合到底加没加 alpha"。

**和评分的天然联姻**：截面模型一次预测整截面 ~309 个 fret12 → `MSE+λ·corr` 里的 corr 就是**截面内 Pearson**，恰好 = 老师 pooled corr / daily-IC 那一维。**预测一个截面、直接优化它的截面排名**——传统结构上做不到的对齐，截面模型白送。

**重正则落地（§十一·11.5）**：时序腿小 GRU（hidden 32–64 / 层≤2）；截面腿 1 块 attention、少头（2–4）、低维；dropout 0.2–0.3；weight_decay ~1e-3；早停；3 seed 平均；置换等变 + 零身份。宁可欠拟合一点，换跨时间稳。

**唯一要动脊柱处**：张量契约从单张 `[L,C]` 泛化成"整截面快照 `[N,L,C]`+mask"——WindowIndexer 从"按 (票,日) 逐窗"改成"按 (date,interval) 聚票 + 各带前 L 窗"，Normalizer 加一档**截面内归一（cross-z，正好补 TCN 瞎掉那维）**。协议/指标/HPO/Orchestrator 全不动（脊柱+卡带架构的价值兑现）。详见规格 §8.2。

## 待决 / 未决问题

- [x] ~~老师测试集是否时间外推~~ → 已澄清：老师保证按时间切 + 隐藏集不重合，**主考跨时间泛化**；是否换股票池**不确定**（偏玄学、倾向不换、规模≈公开集）。鲁棒优先不变。
- [x] ~~带预训练权重是否被允许~~ → 已澄清：**不带权重文件，老师现场重训**；考方法不考权重。假设老师环境有 GPU + PyTorch，fit 可行性不作约束。
- [x] ~~截面建模张量契约泛化方案~~ → **已拍板**（见上「截面模型怎么设计」+ 规格 §8.2）：因子化两腿（共享 GRU 时序腿 + set-attention 截面腿 + 残差）、WindowIndexer 按 `(date,interval)` 聚票、Normalizer 加截面 z。**待实现** `CrossSectionCartridge`（下一程，接已跑通的 SWEEP 协议上）。
- [x] ~~截面置换等变/零身份选型~~ → 定：set-attention（`nn.MultiheadAttention` 1 块、无位置无 ID），§8.2。
- [ ] `MSE+λ·corr` 损失对齐：λ 小网格、batch 内截面 pooled corr 的可微实现（先在 GRU 卡带改 loss，再带进截面卡带）——GRU 基线首跑仍纯 MSE（λ=0），下一晚加。

## 版本记录

| 日期 | 内容 |
|---|---|
| 2026-05-31 | 接 PyTorch 前查真实 h5 定路线：记录①数据实情（无连续 LOB、~59 原始微结构通道、~226 interval/日）→ DeepLOB 退役、改 **TCN-on-原始微结构**；②为什么 TCN（架构是小杠杆、输入/归一化/正则是大杠杆 + 因果自带 + 省 GPU + 归纳偏置对路，Transformer/堆 LSTM 不选的理由）；③RawChannelAdapter 最小语义归一口径（价相对 mid / midpx 日内对数收益 / 量 log1p，为什么不手搓 imbalance） |
| 2026-05-31 | NOTE 重写为 DL 主线笔记（传统/线性全套笔记留传统分支）：记录战略(直接冲0.12)、脊柱+卡带架构、海选+expanding 两段评测(替代三层holdout)、训练生命周期归属、HPO 三旋钮、配置管理(三层枚举+run_id)、喂数据(原始≠裸喂+单张张量+扩展缝)、numpy 参考模型防泄漏 —— 各项"为什么"。完整架构见 `docs/specs/DL实验设计规格.md` |
| 2026-06-01 | **截面模型设计拍板 + 新协议落代码**：① 截面模型 = 因子化两腿（共享 GRU 时序腿 + set-attention 截面腿 + 残差、置换等变零身份、`MSE+λ·corr` 截面对齐），业界 STGNN/关系排序同构、为什么不用时空联合/不用图网络全记上（见「截面模型怎么设计」）；② `AGENTS §十一` 协议落代码 = `Stage.SWEEP` 一命令两档（档1 小网格×近2折×2seed 按最坏折选冠军 → 档2 冠军×5折×3seed）+ `build_dl_folds(fold_select="recent")` 倒贴 Dec 五段 + `enumerate_grid`，34 test 全过 + 真实数据 CLI 跑通；③ GRU-on-433 基线首跑纯 MSE（λ=0），λ·corr 下一晚加 |
| 2026-06-01 | **路线再收敛 + 评测协议重设计**（多轮讨论敲定）：① TCN-on-raw **否决**（真因=截面盲区，非 max_epochs；raw 与 433 同源、无更富数据、raw-LOB 终局不存在）；② **DL 押截面**（时间轴 146 天锁死、截面轴 309/天充裕，数据形状逼出唯一富轴）；③ **还能 alpha 全图**（五条有肉：截面/时序/`MSE+λ·corr`/集成/共因子；"scale 更多数据"因数据锁死已关）；④ **交付=方法非权重** → rolling 每折 = 老师考试的复现 → 评测协议重设计为**锚定扩展 walk-forward + purge/embargo + 最坏折/bootstrap 读数**（规则正文落 `AGENTS.md §十一`）；⑤ DL 时段过拟合风险高于传统、三层防御（rolling 检测 / 重正则+多种子降低 / 集成兜底）。旧"海选+expanding 两段""HPO 三旋钮"两节标历史 |
